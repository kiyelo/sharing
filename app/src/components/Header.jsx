import { CircleCheckIcon, MoreIcon, SearchIcon } from './Icons.jsx'
import { t } from '../i18n.js'
export default function Header({tab,circle,searchOpen,onSearch,onCircleSelect,onCompleted,onManage,lang='ko'}){
 const searchLabel=searchOpen?t(lang,'closeSearch'):t(lang,'search')
 if(tab==='home')return <div className={`head home-head${searchOpen?' search-mode':''}`}><div className="headrow"><h1 className="ttl">{t(lang,'home')}</h1><div className="hicons"><button className="hbtn" data-act="hsearch" onClick={onSearch} aria-label={searchLabel}>{searchOpen?'×':<SearchIcon/>}</button>{!searchOpen&&<button className="hbtn" data-act="sheet-open" onClick={onCompleted} aria-label={t(lang,'completed')}><CircleCheckIcon/></button>}</div></div></div>
 if(tab==='circle')return <div className="head circle-head"><div className="headrow">{circle?<button className="ttlbtn" data-act="circle-pick" onClick={onCircleSelect}><span className="circle-emoji">{circle.emoji}</span><span className="circle-title-window"><span className={`circle-title-text${circle.name.length>10?' stream':''}`}>{circle.name}</span></span><span className="car">⌄</span>{circle.unread>0&&<i className="alert-dot"/>}</button>:<h1 className="ttl">{t(lang,'circle')}</h1>}<div className="circle-tools">{circle&&<p className="head-sub">{lang==='en'?'Members':'멤버'} {circle.members?.length||1}</p>}<div className="hicons"><button className="hbtn" data-act="hsearch" onClick={onSearch} aria-label={searchLabel}>{searchOpen?'×':<SearchIcon/>}</button>{!searchOpen&&<button className="hbtn" data-act="sheet-open" onClick={onCompleted} aria-label={t(lang,'completed')}><CircleCheckIcon/></button>}{circle&&<button className="hbtn" data-act="circle-manage" onClick={onManage} aria-label={t(lang,'manage')}><MoreIcon/></button>}</div></div></div></div>
 return <div className="head more-head"><div className="headrow"><h1 className="ttl">{t(lang,'more')}</h1><div className="hicons"/></div></div>
}
